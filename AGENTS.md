# AGENTS.md

---

## 作業開始時の確認

このプロジェクトで作業を始める前に、まず以下を確認する。

1. `docs/plan/goals.md`
   - プロジェクトの目的、完成形、重視する価値を把握する。

2. `docs/plan/plan.md`
   - 実装方針、優先順位、今後の予定を把握する。

3. `docs/plan/progress.md`
   - 現在の進捗、完了済み作業、未完了作業、注意点を把握する。

4. `docs/changelog.md`
   - これまでの変更履歴（新しい順）。ユーザーから見える機能追加・変更・修正を入れたら、ここにも追記する。

そのうえで、今回の依頼が現在の計画や進捗のどこに関係するかを把握してから作業する。作業内容がこれらの方針と矛盾しそうな場合は、実装前に確認する。

---

## UI に手を入れるとき（必須）

画面の見た目に関わる作業（新しい部品の追加、レイアウト変更、色・文字サイズ・余白の調整、CSS の編集）を行う場合は、**着手前に必ず [docs/design/style-guide.md](docs/design/style-guide.md) を読み、そのルールに従って実装する。**

- 色・タイポグラフィ・余白・角丸・モーション・部品の形は、すべてスタイルガイドで定義済み。独自の値を勝手に導入しない。
- スタイルは `frontend/src/styles.css` に書く（JSX のインラインスタイルは実行時に値が決まる場合のみ）。
- ガイドに無い色やサイズがどうしても必要になったら、`:root` にトークンを追加したうえで **style-guide.md も同時に更新**する。
- ユーザーに見える文字列は `frontend/src/i18n.jsx` に日英そろえて追加し、`t()` 経由で参照する。

アプリ全体でアートスタイルを一貫させることが目的なので、「今回だけ特別な見た目にする」は避ける。

---
